     <td>コメント件数：<%= @book.book_comments.count %></td>
       <% @book.book_comments.each do |book| %>
     <td><%= attachment_image_tag(book.user, :profile_image, :fill, 100, 100, fallback: "no-image-icon.jpg") %></td>
     <td><%= book.user.name %></td>
     <td><%= book.comment %></td>

      <% if book.user == current_user %>
     <td><%= link_to "削除", book_book_comment_path(book.book,book), method: :delete %></td>
      <% end %>

      <% end %>